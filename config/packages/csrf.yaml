# Enable stateless CSRF protection for forms and logins/logouts
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        # Login uses session-backed CSRF (required behind Railway HTTPS proxy).
        stateless_token_ids:
            - submit
