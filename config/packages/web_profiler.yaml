when@dev:
    web_profiler:
        toolbar: true

    framework:
        profiler:
            # Serializer introspection is expensive on large responses; keep dev snappier.
            collect_serializer_data: false

when@test:
    framework:
        profiler: { collect: false }
